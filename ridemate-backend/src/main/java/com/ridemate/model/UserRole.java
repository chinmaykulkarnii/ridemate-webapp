package com.ridemate.model;

public enum UserRole {
    RIDE_PUBLISHER,  // Can publish/offer rides
    RIDE_CONSUMER,   // Can book/consume rides
    ADMIN            // Full system access and analytics
}
